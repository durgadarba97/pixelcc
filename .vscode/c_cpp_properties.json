{
    "env": {
        "myDefaultIncludePath": ["${workspaceFolder}", "${workspaceFolder}/include"]
      },

    "configurations": [
        {
            "name": "Mac",
            "includePath": [
                "./include/**",
                "${workspaceFolder}/include"
            ]
        }
    ],
    "version": 4
}